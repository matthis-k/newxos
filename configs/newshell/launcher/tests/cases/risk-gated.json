[
  {
    "query": "shutdown",
    "expect": ".rows[0].title == \"Shut Down\" and .rows[0].semantics.activation.requiresConfirm == true and .rows[0].executable == false",
    "description": "shutdown should be risk-gated"
  },
  {
    "query": ": shutdown",
    "expect": ".rows[0].title == \"Shut Down\" and .rows[0].semantics.activation.requiresConfirm == true and .rows[0].executable == false",
    "description": ": shutdown should be risk-gated"
  }
]
