[
  {
    "query": "switch",
    "expect": "[.rows[] | select(.title == \"Switch System\" and (.placement == \"promoted-child\" or .placement == \"flattened\") and .semantics.activation.requiresConfirm == true)] | length == 1",
    "description": "Switch System should promote and require confirmation"
  },
  {
    "query": "rebuild",
    "expect": "[.rows[] | select(.title == \"Switch System\" and (.placement == \"promoted-child\" or .placement == \"flattened\") and .semantics.activation.requiresConfirm == true)] | length == 1",
    "description": "rebuild should resolve to Switch System and require confirmation"
  }
]
