[
  {
    "query": "network",
    "expect": ".rows[0].title == \"Networking\" and .rows[0].placement == \"nested-group\" and ((.rows[0].children // []) | length) > 0",
    "description": "Networking should be top expanded group with children"
  },
  {
    "query": "net",
    "expect": ".rows[0].title == \"Networking\" and .rows[0].placement == \"nested-group\" and ((.rows[0].children // []) | length) > 0",
    "description": "net should browse Networking"
  },
  {
    "query": "networking",
    "expect": ".rows[0].title == \"Networking\" and .rows[0].placement == \"nested-group\" and ((.rows[0].children // []) | length) > 0",
    "description": "networking should browse Networking"
  }
]
