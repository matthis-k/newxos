# OpenCode Config

Hand-written OpenCode config fragments that Home Manager installs into `~/.config/opencode/`.

Keep only repo-owned config here. Do not keep local package manager state like `node_modules/` or lockfiles in this directory.
