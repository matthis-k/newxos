[package]
name = "newxos"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "newxos"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4"
serde_json = "1"

[features]
default = ["completions"]
completions = []
